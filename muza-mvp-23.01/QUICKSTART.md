
# 🚀 Быстрый старт: Muza Aura OS v6.7 "Nexus"

Добро пожаловать в обновленную экосистему Aura OS! Этот документ поможет вам быстро начать работу с приложением на любой платформе.

## 📋 Что было улучшено

### ✨ Новые возможности
1.  **PWA поддержка:** Теперь приложение можно установить на мобильные устройства (iOS/Android) и десктопы прямо из браузера.
2.  **Desktop-приложение:** Создана полноценная сборка для Windows, macOS и Linux с помощью Electron.
3.  **Установочные скрипты:** Файлы `install.bat` и `install.sh` автоматизируют всю настройку.
4.  **Полный оффлайн-режим:** Service Worker кэширует приложение, а AI переключается на локальные модели без интернета.

### 🐛 Исправления
1.  **Конфликт зависимостей:** Проблема с `vite-plugin-pwa` решена.
2.  **"Темный экран" в Electron:** Проблема с загрузкой production-сборки в Electron исправлена.
3.  **Структура проекта:** Устранены дубликаты файлов, проект приведен к единой структуре.

### ⚙️ Оптимизация
1.  **Адаптивный UI:** Улучшена работа интерфейса на мобильных устройствах.
2.  **Производительность:** Добавлены утилиты для оптимизации и улучшена логика AI-фолбэка.

---

## 🛠 Шаг 1: Установка

> **Важно:** У вас должен быть установлен [Node.js](https://nodejs.org/) (версия 18+).

Откройте терминал в корневой папке проекта и запустите скрипт для вашей ОС.

#### Для Windows:
Откройте PowerShell или Command Prompt и выполните:
```cmd
.\install.bat
```

#### Для macOS / Linux:
Откройте терминал и выполните:
```bash
chmod +x install.sh
./install.sh
```
Скрипт установит все необходимые зависимости для веб-версии и десктоп-приложения.

---

## 🖥️ Шаг 2: Запуск в режиме разработки

### Веб-версия (для браузера и мобильных)
```bash
npm run dev
```
- Откройте `http://localhost:5173` в браузере.
- Для тестирования на телефоне, убедитесь, что он находится в той же Wi-Fi сети, и откройте сетевой адрес, указанный в терминале (например, `http://192.168.1.5:5173`).

### Desktop-приложение (Electron)
```bash
npm run electron:dev
```
- Откроется нативное окно приложения с доступом к DevTools (Ctrl+Shift+I).

---

## 📦 Шаг 3: Сборка и распространение

### Сборка веб-версии
```bash
npm run build
```
- Результат будет в папке `dist/`. Загрузите ее на любой статический хостинг.
- **Для работы PWA требуется HTTPS.**

### Сборка Desktop-приложения
Выберите команду для вашей целевой платформы:
```bash
# Для Windows
npm run electron:build:win

# Для macOS
npm run electron:build:mac

# Для Linux
npm run electron:build:linux
```
- Готовые установочные файлы `.exe`, `.dmg` или `.AppImage` появятся в папке `dist-electron/`.

---

## 📱 Шаг 4: Установка PWA на телефон

1.  Запустите веб-версию (`npm run dev`) и откройте сетевой URL на вашем телефоне.
2.  **На Android (Chrome):** Появится всплывающее окно "Добавить на главный экран" или найдите этот пункт в меню браузера.
3.  **На iOS (Safari):** Нажмите кнопку "Поделиться" и выберите "На экран 'Домой'".

Приложение будет установлено и появится на рабочем столе как обычное приложение.

---

## 🧠 Настройка AI

- **Онлайн (Gemini):** Чтобы использовать Gemini, создайте файл `.env` в корне проекта и добавьте:
  `VITE_GEMINI_API_KEY=ВАШ_КЛЮЧ`
- **Офлайн (Ollama):** Если `.env` пуст или отсутствует, система попытается подключиться к локальному серверу Ollama по адресу `http://127.0.0.1:11434`.
- **Автономный режим (MuzaAI):** Если и Gemini, и Ollama недоступны, будет использоваться встроенный, самый простой AI.

---

Теперь Aura OS готова к работе на любой платформе. Наслаждайтесь!
